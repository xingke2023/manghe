<view class="empty">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-text">{{text}}</text>
  <text wx:if="{{hint}}" class="empty-hint">{{hint}}</text>
  <view wx:if="{{action}}" class="btn-sm empty-action" bindtap="onAction">{{action}}</view>
</view>
