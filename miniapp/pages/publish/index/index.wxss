.gate {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 32rpx;
}
.gate-text {
  font-size: 26rpx;
  color: var(--color-text-hint);
}
